Cargo.lock 是自动生成的

# 构建(开发的时候用)
cargo build
# 编译时会进行优化
# 代码运行的更快，但是编译时间更长
# 会在target/release 目录下而不是在target/debug 目录
cargo build --release

# 构建并运行，将两步合并到一起了。
cargo run 

# 检查 代码，确保能通过编译，但是不产生任何可执行文件
# cargo check 要比cargo build 快得多
# 编写代码的时候可以连续反复的使用cargo check 检查 代码，提高效率
cargo check 
